/*
Clamps a value to a given min/max, returning either min or max if the
value is less than or greater than either. Returns the value if it is
within the given range.
*/
float clamp(float val, float min, float max);